# 2026-08-18 日志

## Day 5.1 战斗状态机完成
- 创建 `Enums/ABDCombatEnums.h` + `.cpp`，定义 `ECombatState` 枚举（Idle/Moving/Attacking/HitReacting/Dead）
- `BattleCharacter.h` 加 include + `CurrentCombatState` 变量 + `SetCombatState` 声明
- `BattleCharacter.cpp` 实现 `SetCombatState`（带日志），Attack/OnAttackMontageEnded 里调用
- 删除冗余的 `bIsAttacking` 布尔变量，完全用状态机替代
- 修复 `#include` 顺序问题：`.generated.h` 必须是最后一个 include

## DebugHelper 工具类
- 新建 `Components/DebugHelper/`，封装 `UDebugHelper : UBlueprintFunctionLibrary`
- 两个静态方法：`PrintOnScreen`（仅屏幕）和 `DebugLog`（屏幕+日志）
- 替换了 HealthComponent.cpp 和 BattleCharacter.cpp 中所有运行时 UE_LOG 为 UDebugHelper::DebugLog
- GE 加载失败的启动错误日志保留 UE_LOG（不适合屏幕显示）
