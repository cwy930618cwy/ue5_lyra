# 2026-08-20

## Git 回退 + 教程更新
- 用户回退到 commit 98974e9b（受击蒙太奇动画），使用 `git reset --hard`
- 更新了 `AI_Learning_Notes/04_战斗系统/20260805_战斗系统架构设计.md`：
  - 进度表：受击动画从"🔄 进行中"改为"✅ 完成"，新增血条UI和属性钳制为已完成
  - 新增"当前架构总览"章节：完整的组件树、输入绑定、数据流说明
  - 新增"关键设计决策"章节：受击禁止攻击、蒙太奇播放/停止时机、GE流程原因、HealthComponent独立设计

## 属性集扩展（T2.3）
- HealthSet 新增 5 个属性：AttackPower / Defense / Stamina / MaxStamina / MoveSpeed
- PreAttributeChange 加 Stamina 钳制 [0, MaxStamina]
- BattleCharacter 加 ShowStats() 方法（K键打印所有属性，FColor::Cyan）
- 新建 IA_ShowStats 输入动作并绑定到 IMC_Default

## GAS 技能系统教学（GA_BattleAbility + GA_Attack）
- 创建目录结构：Abilities/GA_BattleAbility/、Abilities/GA_Attack/
- GA_BattleAbility 基类：InstancingPolicy=InstancedPerActor（Lyra做法）、NetExecutionPolicy=LocalPredicted
- GA_Attack：ActivateAbility 播蒙太奇、EndAbility 打日志
- ⚠️ 踩坑：UE5.5 API 变更
  - `PerExecution` → `InstancedPerExecution`（枚举名变更）
  - `FGameplayAbilityTriggerData(Source)` 构造函数 → `FAbilityTriggerData` + 分开赋值（类型改名+构造方式变更）
  - 跨子目录 include 需要相对路径 `../GA_BattleAbility/GA_BattleAbility.h`
  - .cpp 里用 ACharacter::GetMesh() 必须 include `GameFramework/Character.h`
  - FAbilityTriggerData 需要 include `Abilities/GameplayAbilityTypes.h`
- 用户明确要求：**P0 规范 — 所有教学代码必须用 UE5.5+ 新 API，禁止旧版写法**
- 已更新 MEMORY.md 加入 P0 规范和已知 API 变更清单

## Lyra 完整架构解析 + AgentFlow 同步
- 深度阅读 Lyra 源码，生成 `AI_Learning_Notes/03_GAS技能系统/20260820_Lyra完整架构解析.md`
- 覆盖：ASC放PlayerState、AbilitySet批量授予、InputTag绑定链路、PlayMontageAndWait、Experience/PawnData、InitState四阶段、Fragment组合模式、命名规范
- 连接 AgentFlow MCP (127.0.0.1:9080/mcp) 上报 4 条经验：
  - exp_id 1787206515734-99e66043: Lyra GAS架构核心（positive/architecture）
  - exp_id 1787206522197-daea2716: Experience系统+InitState（positive/architecture）
  - exp_id 1787206528768-dccd245e: 装备物品系统+命名规范（positive/architecture）
  - exp_id 1787206536164-f215b310: UE5.5 API变更踩坑（negative/bug）
- skill_type 统一用 `ue5_lyra_architecture` 和 `ue5_cpp_teaching`
- report_experience 必需字段：agent_id/skill_type/type/category/description(≥50字)
- GA_Attack 改为从角色 GetAttackMontage() getter 获取蒙太奇（避免 protected 访问 C2248）

## AgentFlow MCP 进度同步（第二次）
- P2 goal_id = `goal_19d9b7537c8_29f692dc`（P2 攻击与伤害）
- 创建 3 个新任务（create_tasks 需 tasks 数组字段）：
  - task_1a01de07fad_61fc73a8: T2.4 GA基类 GA_BattleAbility
  - task_1a01de07fad_bd09fffd: T2.5 攻击技能 GA_Attack
  - task_1a01de07fad_22e998e0: T2.6 授予技能+输入触发
- P2 进度更新为 50%
- **AgentFlow 关键操作规范**：
  - update_task 的 status/progress 字段不生效（接口限制）
  - 必须先用 claim_task 认领任务，再用 update_task_progress 更新进度
  - create_tasks 需要 tasks 数组，不能传单个 task 对象
  - report_task_result 需要 status 字段
